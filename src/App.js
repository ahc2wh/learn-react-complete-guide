import './App.css';

function App() {
  return (
    // this content is not html but JSX (react self made term)
    // JSX will auto transcribe it to javascript
    // allows writing html in quotation marks
    <h1>Hi I'm a react app, removing some boiler plate</h1>
  );
}

export default App;
// ES6 feature - if you import this whole file, you simply import this class becuase its the default export